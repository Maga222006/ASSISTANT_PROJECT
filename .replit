run = "pip install --no-cache-dir -r requirements.txt && uvicorn agent:app --host 0.0.0.0 --port 8080"
language = "python3"
entrypoint = "agent.py"

[nix]
channel = "stable-22_11"

[env]
PYTHONPATH = "." 