run = "pip install uvicorn fastapi && pip install -r requirements.txt && ~/.local/bin/uvicorn agent:app --host 0.0.0.0 --port 8080"
language = "python3"
entrypoint = "agent.py"

[nix]
channel = "stable-22_11"

[env]
PYTHONPATH = "."
PATH = "/home/runner/.local/bin:${PATH}" 